<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.dream.dreamview"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".Dialog"
                  android:theme="@style/MyDialogStyleBottom"/>

        <activity android:name=".sample.NavFoldSampleActivity"/>
        <activity android:name=".sample.CustomNavFoldSampleActivity"/>
        <activity android:name=".SwipActivity"/>
        <activity android:name=".sample.CustomeToolbarSampleActivity"/>
        <activity android:name=".test.RecyclerViewActivity"/>
        <activity android:name=".test.RetrofitActivity"/>
        <activity android:name=".module.meinv.BeautyActivity" android:process=":test"/>
        <activity android:name=".module.meinv.FullScreenActivity"/>
        <activity android:name=".module.multi.MultiTypeActivity"/>
        <activity android:name=".module.video.VideoActivity"/>

    </application>

</manifest>